# ========== 必填配置 ==========
# Ave.ai API密钥（从Ave.ai获取）
AVE_AI_API_KEY=your_ave_ai_api_key_here

# ========== 可选配置 ==========

# API配置
AVE_AI_BASE_URL=https://prod.ave-api.com    # Ave.ai API V2端点
API_TIMEOUT=10000                           # API超时时间（毫秒）
API_MAX_RETRIES=3                          # 最大重试次数
API_RETRY_DELAY=1000                       # 重试延迟（毫秒）

# 缓存配置（重要：控制播报频率）
CACHE_ADDRESS_TIMEOUT=10000                # 地址播报缓存时间（毫秒，默认10秒防刷屏）
CACHE_CONTRACT_TIMEOUT=600000              # API数据缓存时间（毫秒，默认10分钟）
MAX_CACHE_SIZE=1000                        # 最大缓存数量
MIN_QUERY_INTERVAL=0                       # 同地址最小查询间隔（毫秒，0表示无限制）

# 速率限制配置
MAX_REQUESTS_PER_MINUTE=30                 # 每分钟最大请求数

# 机器人配置
BOT_NAME=bsc-contract-bot                  # 机器人名称
BOT_PUPPET=wechaty-puppet-wechat4u         # Puppet类型
MONITOR_ROOMS=                             # 监控的群名称（逗号分隔，留空则监控所有群）

# 日志配置
LOG_LEVEL=info                             # 日志级别（debug, info, warn, error）

# 消息配置
SHOW_CONTRACT_ADDRESS=false                # 是否在播报中显示合约地址
SEND_ERROR_TO_ROOM=true                   # 错误消息是否发送到群